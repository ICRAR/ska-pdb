%table
  %thead
    %th Parameter
    %th
  %tbody
    - cart.line_items.each do |item|
      %tr
        %td{:title=>"#{item.parameter.name}"}
          = truncate(item.parameter.name, :length => 26)
        %td
          = button_to "\u00d7", line_item_path(item.id), :class => 'remove-item', :method => :delete, :remote => true
